# 00-setup

## VS Code essentials
- Extensions: Live Server, ESLint, Prettier
- Settings:
  - Format On Save: ON
  - Default Formatter: Prettier

## Local server (option A: VS Code)
Right-click `index.html` → **Open with Live Server**.

## Local server (option B: Python)
```bash
python -m http.server 8000
# then open http://localhost:8000
```

## Folder naming
- Use `kebab-case` for folders/files: `my-component`, `todo-app`
